{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "Access Key"|t('aliyun'),
    instructions: "",
    required: true,
    name: "accessKey",
    value: settings.accessKey,
    suggestEnvVars: true
}) }}

{{ forms.autosuggestField({
    label: "Secret Key"|t('aliyun'),
    instructions: "",
    required: true,
    id: 'secretKey',
    name: "secretKey",
    value: settings.secretKey,
    suggestEnvVars: true
}) }}

{{ forms.lightSwitchField({
    label: "Allow Volume Auth Settings"|t('aliyun'),
    instructions: "Allow a OSS Volume set its Access Key nad Secret Key."|t('aliyun'),
    required: false,
    id: 'allowVolumeAuthSettings',
    name: "allowVolumeAuthSettings",
    on: settings.allowVolumeAuthSettings,
}) }}